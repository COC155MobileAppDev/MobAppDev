<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.team05.lecturec" >

    <uses-permission
        android:name="com.example.team05.lecturec.myProviderPermission">
    </uses-permission>

    <!-- ****Uncomment if DB interaction doesn't work****
    <uses-permission
        android:name="android.permission.READ_CONTACTS">
    </uses-permission>
    -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ViewControllers.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name=".ModelControllers.DBProvider"
            android:authorities="com.example.team05.lecturec"
            android:enabled="true"
            android:permission="com.example.team05.lecturec.myProviderPermission">
        </provider>
    </application>



</manifest>
